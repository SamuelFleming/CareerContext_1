# 006.2 — Dashboard Requirements and Data Contract

## Status

**Complete** — contract locked; implemented by **006.3** / **006.4**.

## Ticket Type

Design / Requirements / API Planning

## Phase

Phase 1 — Auth and Profile

## Related Screen

```
4. Dashboard
```

## Depends On

- **006.1** — Dashboard audit (completed)
- **007.2** — `CoreContext` model and profile API shape

## Objective

Define the reconciled Dashboard product requirements, widget behaviour, and `GET /api/dashboard` response contract — aligned with the Interactive CV layout from **006.1** and the `CoreContext` data model from **007.2**.

This ticket must be complete before **006.3** and **006.4**.

---

## Reconciliation Summary (006.1 vs earlier draft)

| Topic | Earlier 006.2 draft | After 006.1 + 007.2 |
|-------|---------------------|---------------------|
| Layout | Separate widgets (welcome, previews, quick actions, placeholders) | **Interactive CV hero** + conditional completeness + collapsible right column |
| Identity fields | `User.name`, `User.headline` | `CoreContext.fullName`, `CoreContext.headline`; `User.email` |
| Context summary | `coreContextMd`, `coreContextUpdatedAt` | `CoreContext.rawSummaryMd`, `CoreContext.summaryUpdatedAt` |
| Completeness widget | Always visible scored panel | **Shown only when incomplete** (`showPrompt: true`) |
| Evidence Summary | Static placeholder card | **Collapsible panel** (alternates with Recent Activity) |
| Recent Activity | Generic placeholder | Top 5 recent evidence items (Phase 2+); empty state in Phase 1 |
| Skill chips on CV | Not specified | Phase 1 mock data (`phase1DashboardMocks`); Phase 2 derived from Experiences |
| Experience cards on CV | Not in API contract | Phase 1 mock data; Phase 2 top 2–3 experiences |
| Core resume on Dashboard | Separate preview card | **Removed** — resume edited on Profile only |
| Quick actions panel | Listed in early draft | **Removed** from Dashboard UI |
| API envelope | Flat top-level keys | `{ data: { ... } }` per project API conventions |

---

## Dashboard Product Role

The Dashboard answers:

1. **Who am I here?** — personalised Interactive CV identity
2. **Is my core context ready?** — conditional completeness prompt
3. **What does my career narrative look like?** — Core Context summary preview inside the CV
4. **What evidence am I building?** — placeholder / future experience highlights
5. **What have I worked on recently?** — collapsible recent-evidence panel (Phase 2+)

Opportunities and documents are **out of scope** for Phase 1 dashboard data.

---

## Layout Contract

### Left column — Interactive CV (`interactiveCv`)

Primary card styled like a living CV (matches existing `DashboardPage` visual language).

| Section | Phase 1 behaviour | Data fields |
|---------|-------------------|-------------|
| Header | Name + headline | `identity.fullName`, `identity.headline` |
| Contact strip | Optional one-line contact | `identity.email`, `identity.mobile`, `identity.location` |
| Profile summary | Preview of raw summary or empty-state prompt | `interactiveCv.summaryPreview`, `interactiveCv.summaryUpdatedAt`, `interactiveCv.reviewSuggested` |
| Core competencies | Skill chips from Phase 1 mock data | `interactiveCv.coreCompetencies[]` |
| Experience highlights | Evidence cards from Phase 1 mock data | `interactiveCv.highlightExperiences[]` |

### Right column

#### A. Profile completeness (`profileCompleteness`)

- Rendered **only when** `profileCompleteness.showPrompt === true`
- Hidden when profile is sufficiently complete (`status: "ready"`)

#### B. Collapsible evidence panel (`evidencePanel`)

Single container toggling between two views:

| View | Phase 1 | Phase 2+ |
|------|---------|----------|
| `evidenceSummary` | Empty state: evidence collection comes next | Compact experience/activity counts or highlights |
| `recentActivity` | Empty state: recent updates appear here | Top 5 items by `updatedAt` (Activities + Journal entries) |

Default expanded view: `evidenceSummary`.

---

## Profile Completeness Rules

Calculated on the **backend** by `server/src/services/profileCompletenessService.js` and returned on both `GET /api/dashboard` and `GET /api/profile`.

| Field | Source | Weight | Complete when |
|-------|--------|--------|---------------|
| Full name | `CoreContext.fullName` | 20% | non-empty trimmed string |
| Headline | `CoreContext.headline` | 20% | non-empty trimmed string |
| Location | `CoreContext.location` | 20% | non-empty trimmed string |
| Career summary | `CoreContext.rawSummaryMd` | 20% | non-empty trimmed string |
| Core resume | `User.coreResumeMd` | 20% | non-empty trimmed string |

**Score:** `Math.round((completed / total) * 100)` where `total` is 5.

**Status:**

| Condition | Status |
|-----------|--------|
| `completed === 0` | `empty` |
| `completed === total` | `ready` |
| otherwise | `in_progress` |

**`showPrompt`:** `true` while `completed < total`.

**`checks`:** array of `{ key, label, done }` for UI checklist.

**`missing`:** array of incomplete field keys.

**`nextAction`:** first incomplete field mapped to a label + `/profile` route.

---

## Core Context Preview Rules

Used inside the Interactive CV profile summary section.

- **Preview length:** first 250–400 characters of `rawSummaryMd`
- **Truncation:** append `…` when truncated
- **Empty:** show guided empty-state copy linking to `/profile`
- **Staleness:** `reviewSuggested: true` when `summaryUpdatedAt` is older than 30 days (UX hint only)
- **Timestamp:** display `summaryUpdatedAt` when present

---

## Phase 1 Interactive CV Mocks

Until Experience APIs exist, competencies and highlights are served from `server/src/constants/phase1DashboardMocks.js`. The client mirrors these in `client/src/features/dashboard/phase1MockData.js` as a fallback when the API returns empty arrays.

---

## Recommended Endpoint

```
GET /api/dashboard
```

- Authenticated (`Authorization: Bearer <token>`)
- Single compact payload for Screen 4
- Does **not** replace `GET /api/profile` (profile editing stays on Screen 5)

---

## Response Contract

```json
{
  "data": {
    "identity": {
      "fullName": "Sam Fleming",
      "headline": "Graduate Developer",
      "email": "sam@example.com",
      "mobile": "+61 400 000 000",
      "location": "Brisbane, AU"
    },
    "profileCompleteness": {
      "score": 60,
      "completed": 3,
      "total": 5,
      "status": "in_progress",
      "showPrompt": true,
      "checks": [
        { "key": "fullName", "label": "Full name", "done": true },
        { "key": "headline", "label": "Headline", "done": true },
        { "key": "location", "label": "Location", "done": false },
        { "key": "rawSummaryMd", "label": "Career summary", "done": true },
        { "key": "coreResumeMd", "label": "Core resume", "done": false }
      ],
      "missing": ["location", "coreResumeMd"],
      "nextAction": {
        "label": "Add your location",
        "to": "/profile"
      }
    },
    "interactiveCv": {
      "summaryPreview": "I am currently working in a full-stack .NET public sector role...",
      "summaryUpdatedAt": "2026-06-12T00:00:00.000Z",
      "reviewSuggested": false,
      "coreCompetencies": ["React", "Node.js", "Leadership", "Express"],
      "highlightExperiences": [
        {
          "id": "mock-exp-1",
          "type": "job",
          "title": "Full-Stack Developer Intern",
          "meta": "TechCorp · Jun 2025 – Present",
          "description": "Led an internal dashboard rebuild used by 40+ staff.",
          "tags": ["React", "Node.js", "Leadership"]
        }
      ]
    },
    "evidencePanel": {
      "defaultView": "evidenceSummary",
      "evidenceSummary": {
        "status": "placeholder",
        "message": "Experience evidence comes next.",
        "counts": {
          "experiences": 0,
          "activities": 0,
          "journalEntries": 0
        }
      },
      "recentActivity": {
        "status": "placeholder",
        "message": "Your latest captured evidence will appear here.",
        "items": []
      }
    },
    "phasePlaceholders": {
      "experienceEvidence": "planned",
      "opportunities": "planned",
      "documents": "planned"
    }
  }
}
```

### `highlightExperiences` item shape

```json
{
  "id": "mock-exp-1",
  "type": "job",
  "title": "Full-Stack Developer Intern",
  "meta": "TechCorp · Jun 2025 – Present",
  "description": "Led an internal dashboard rebuild used by 40+ staff.",
  "tags": ["React", "Node.js", "Leadership"]
}
```

Phase 2+ will use real `Experience` IDs and fields; `description` may map from experience overview.

### `recentActivity.items` shape (Phase 2+)

```json
{
  "id": "itemId",
  "kind": "activity",
  "title": "OMS Joint Applicant Unlink Screen",
  "meta": "Updated 2 days ago",
  "updatedAt": "2026-06-10T00:00:00.000Z",
  "href": "/activities/activityId"
}
```

---

## Technical Tasks

1. Confirm Interactive CV layout matches **006.1** target (not widget-soup layout).
2. Map all dummy `DashboardPage` fields to API contract keys.
3. Align field names with `CoreContext` + `User` models.
4. Document completeness, preview, and staleness rules (above).
5. Update `docs/08_api_contract.md` API-004 section to match this contract.
6. Update `docs/06_screen_catalogue_and_data_requirements.md` Screen 4 section.

---

## Acceptance Criteria

- [x] Layout contract reflects Interactive CV + collapsible right column from **006.1**
- [x] All dummy dashboard fields classified: API now / calculated / Phase 2 placeholder / Phase 1 mock
- [x] Profile completeness uses shared `profileCompletenessService` rules
- [x] `showPrompt` conditional behaviour documented
- [x] `GET /api/dashboard` response shape is fully specified
- [x] Core resume preview and quick actions removed from Dashboard scope
- [x] **006.3** and **006.4** implemented

## Suggested Commit Message

```
docs(dashboard): reconcile dashboard requirements with 006.1 audit and CoreContext model
```
