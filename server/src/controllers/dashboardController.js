// server/src/controllers/dashboardController.js

const { notImplemented } = require('../utils/notImplemented');

const getDashboard = notImplemented(
  'GET /api/dashboard',
  'Load dashboard summary including profile status, counts, and recent workspace items'
);

module.exports = {
  getDashboard,
};